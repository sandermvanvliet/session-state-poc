web01:
  build: .
  expose:
    - 80
  links:
    - mysql01

web02:
  build: .
  expose:
    - 80
  links:
    - mysql02

haproxy:
  image: haproxy
  volumes:
    - ./haproxy.cfg:/usr/local/etc/haproxy/haproxy.cfg
  links:
    - web01
    - web02
  ports:
    - "8080:80"
    - "70:70"
  expose:
    - "8080"
    - "70"

mysql01:
  image: mysql
  environment:
    MYSQL_ROOT_PASSWORD: 123456
    MYSQL_USER: dev
    MYSQL_PASSWORD: 123456
    MYSQL_DATABASE: app

mysql02:
  image: mysql
  environment:
    MYSQL_ROOT_PASSWORD: 123456
    MYSQL_USER: dev
    MYSQL_PASSWORD: 123456
    MYSQL_DATABASE: app
